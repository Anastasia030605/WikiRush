# WikiRush

WikiRush - это игра-гонка по Википедии, где игроки соревнуются в поиске кратчайшего пути между статьями, переходя по ссылкам внутри статей.

## Описание

WikiRush предлагает уникальный игровой опыт, основанный на структуре Википедии. Игроки начинают с одной статьи и должны найти путь к целевой статье, используя только ссылки внутри статей. Побеждает тот, кто найдет путь быстрее или за меньшее количество шагов.

### Основные возможности

- Случайная генерация начальной и целевой статей с гарантированным путём между ними
- Интеграция с русской Википедией
- Мультиплеер с real-time обновлениями через WebSocket
- Различные режимы игры (одиночный, мультиплеер)
- Ограничение на создание игр (не более 3 незавершенных игр одновременно)
- Отмена игр создателем до их завершения
- Просмотр доступных ссылок из текущей статьи
- Краткие описания статей для принятия решений
- Таблица лидеров
- Система достижений
- Профили игроков с аватарами
- Статистика игроков
- Архив завершенных и отмененных игр
- Фильтрация игр по участию пользователя

## Технологии

### Backend
- **FastAPI** - современный асинхронный веб-фреймворк
- **SQLAlchemy 2.0** - ORM с поддержкой async/await
- **SQLite** - база данных для разработки (PostgreSQL для production)
- **Pydantic** - валидация данных и схемы
- **JWT** - аутентификация пользователей
- **WebSocket** - real-time обновления игр
- **httpx** - асинхронные HTTP запросы к Wikipedia API

### Frontend
- **Svelte** - современный реактивный фреймворк
- **Vite** - быстрый сборщик модулей
- **Axios** - HTTP клиент для API запросов
- **WebSocket** - real-time соединение с сервером

## Структура проекта

```
WikiRush/
├── backend/           # FastAPI backend
│   ├── app/
│   │   ├── api/      # API endpoints
│   │   │   └── v1/   # API версии 1
│   │   ├── core/     # Конфигурация, БД, безопасность
│   │   ├── models/   # SQLAlchemy модели
│   │   ├── schemas/  # Pydantic схемы
│   │   └── services/ # Бизнес-логика
│   ├── tests/        # Тесты
│   ├── uploads/      # Загруженные файлы (аватары)
│   └── README.md
├── frontend/         # Svelte frontend приложение
│   ├── public/       # Статические файлы
│   ├── src/
│   │   ├── lib/      # Компоненты и утилиты
│   │   └── routes/   # Страницы приложения
│   └── README.md
└── README.md
```

## Быстрый старт

### Требования

- Python 3.9+
- Node.js 16+
- SQLite (для разработки) или PostgreSQL (для production)

### Установка и запуск

#### Backend

1. Клонируйте репозиторий:
```bash
git clone https://github.com/YOUR_USERNAME/wikirush.git
cd wikirush
```

2. Перейдите в директорию backend и создайте виртуальное окружение:
```bash
cd backend
python -m venv venv

# Linux/Mac
source venv/bin/activate

# Windows
venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
# Backend будет использовать SQLite по умолчанию
# Для production настройте PostgreSQL
```

5. Запустите сервер:
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

6. Откройте документацию API:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

#### Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev-сервер:
```bash
npm run dev
```

4. Откройте приложение в браузере:
- http://localhost:5173

## Игровой процесс

1. **Регистрация/Вход** - создайте аккаунт или войдите
2. **Создание игры** - выберите режим и параметры игры (или используйте случайные статьи)
   - Максимум 3 незавершенных игры одновременно
   - Возможность отменить игру до её старта
3. **Присоединение** - пригласите друзей или присоединитесь к существующей игре
4. **Старт** - создатель запускает игру
5. **Навигация** - переходите по ссылкам в статьях, пытаясь достичь цели
   - Просмотр доступных ссылок из текущей статьи
   - Краткое описание статей при наведении
   - Real-time обновления ходов других игроков
6. **Победа** - первый, кто достигнет целевой статьи, побеждает
7. **Достижения** - зарабатывайте достижения за различные успехи
8. **Архив** - просматривайте историю завершенных и отмененных игр

## Особенности

### Управление играми
- **Ограничение на создание**: Каждый пользователь может создать не более 3 незавершенных игр одновременно
- **Отмена игр**: Создатель может отменить игру в любой момент до её завершения
- **Фильтрация**: В списке игр отображаются только игры, к которым вы можете присоединиться или в которых участвуете
- **Приватность**: Одиночные игры других пользователей скрыты из общего списка

### Real-time обновления
- Мгновенное обновление состояния игры через WebSocket
- Уведомления о ходах других игроков
- Автоматическое обновление счетчика шагов
- Уведомления об отмене игры

### Система достижений
- Разнообразные достижения за различные игровые успехи
- Отслеживание прогресса по достижениям
- Отображение достижений в профиле пользователя

## API Endpoints

### Аутентификация
- `POST /api/v1/auth/register` - Регистрация
- `POST /api/v1/auth/login` - Вход
- `POST /api/v1/auth/refresh` - Обновление токена

### Игры
- `POST /api/v1/games` - Создать игру (макс. 3 незавершенных игры)
- `GET /api/v1/games` - Список игр (с фильтрацией по участию)
- `GET /api/v1/games/random-articles` - Получить случайные статьи с гарантированным путём
- `GET /api/v1/games/{id}` - Информация об игре
- `GET /api/v1/games/{id}/available-links` - Доступные ссылки из текущей позиции
- `POST /api/v1/games/{id}/join` - Присоединиться к игре
- `POST /api/v1/games/{id}/start` - Запустить игру
- `POST /api/v1/games/{id}/move` - Сделать ход
- `POST /api/v1/games/{id}/cancel` - Отменить игру (только создатель)
- `WS /api/v1/games/{id}/ws` - WebSocket для real-time обновлений

### Достижения
- `GET /api/v1/achievements` - Список всех достижений
- `GET /api/v1/achievements/user/{user_id}` - Достижения пользователя

### Wikipedia
- `GET /api/v1/wikipedia/article/{title}/summary` - Краткое описание статьи
- `GET /api/v1/wikipedia/article/{title}/links` - Список ссылок из статьи
- `GET /api/v1/wikipedia/search` - Поиск статей

### Пользователи
- `GET /api/v1/users/me` - Текущий пользователь
- `PATCH /api/v1/users/me` - Обновить профиль
- `POST /api/v1/users/me/avatar` - Загрузить аватар
- `GET /api/v1/users/{id}` - Информация о пользователе
- `GET /api/v1/users/{id}/stats` - Статистика пользователя

### Лидерборд
- `GET /api/v1/leaderboard` - Таблица лидеров

## Разработка

Подробная информация о разработке доступна в [backend/README.md](backend/README.md)

### Тестирование

```bash
cd backend
pytest
```

### Линтинг и форматирование

```bash
cd backend
# Форматирование
black .

# Линтинг
ruff check .

# Проверка типов
mypy app/
```

## Лицензия

MIT

## Контакты

Если у вас есть вопросы или предложения, создайте issue в репозитории.
